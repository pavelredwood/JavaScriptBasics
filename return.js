function actualIphone () {
    phoneName = "iPhone 14 Pro Max";
    return phoneName;
}

function random (number) {
    const result = Math.floor(Math.random() * number);
    return result;
}

function randomShort(number) {
    return Math.floor(Math.random() * number);
  }